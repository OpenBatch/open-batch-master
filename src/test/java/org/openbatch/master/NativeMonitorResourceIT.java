package org.openbatch.master;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeMonitorResourceIT extends MonitorResourceTest {

    // Execute the same tests but in native mode.
}